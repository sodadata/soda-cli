Full Command Tree (WIP)

  soda
  │
  │  Global flags:
  │    --output table|json|csv   (auto-detects TTY)
  │    --profile <name>
  │    --no-color / --quiet / --verbose
  │    --no-interactive           (for CI/CD and AI agents)
  │
  ├── auth
  │   ├── login   [--profile <name>]    # wizard → API key → test → save
  │   ├── logout  [--profile <name>]
  │   ├── status                        # active profile, org, connection health
  │   └── switch  <profile>
  │
  ├── init                              # scaffold soda.yml + configs/ + contracts/
  │
  ├── run  [<file-or-dir>]              # smart pipeline: resolves DS from contract, verify + publish
  │   --agent                          # delegate to Soda Agent
  │   --parallel                       # run directory of contracts concurrently
  │   --datasource <file>              # override: explicit DS config (no soda.yml needed)
  │   # exit codes: 0=pass 1=checks failed 2=error 3=auth error
  │
  ├── status                            # org dashboard: datasets, checks, incidents, recent scans
  │
  ├── datasource  (alias: ds)
  │   ├── create      [--type pg|sf|bq|duck|...]   # local YAML config
  │   ├── connect     --agent <name> --type <t>    # cloud-registered, via Soda Agent
  │   ├── diagnostics <id>                          # view/configure diagnostics warehouse
  │   ├── test        [<name-or-file>]
  │   └── list
  │
  ├── contract
  │   ├── create    --dataset <ds/db/schema/table>  # bootstrap from live schema
  │   ├── validate  [<file>]                         # syntax check, no network
  │   ├── publish   [<file>]
  │   ├── fetch     --dataset <fqn>
  │   ├── diff      [<file>]  --dataset <fqn>
  │   ├── suggest   --dataset <fqn>                  # Copilot: propose additions
  │   ├── generate  --dataset <fqn>                  # Autopilot: full contract from scratch
  │   ├── verify    [<file>]                         # explicit, all flags required
  │   │   --agent / --publish / --set / --check / --watch
  │   └── request                                    # PR flow for published contracts
  │       ├── list    [--status open|done|all]
  │       ├── fetch   <id>  [--proposal <n>]
  │       ├── push    <id>  [<file>]  [--message]
  │       └── close   <id>  [--status done|wontdo]
  │
  ├── scan
  │   ├── get   <id>
  │   └── logs  <id>  [--follow]
  │
  ├── checks
  │   ├── list    [--dataset <id>] [--status passing|failing|all]
  │   └── delete  <id>
  │
  ├── dataset
  │   ├── list         [--filter <query>] [--tag <tag>]
  │   ├── update       <id>  [--name] [--tag] [--attr key=value]
  │   ├── delete       <id>
  │   ├── profiling    <id>
  │   ├── warehouse    <id>               # compute + diagnostics warehouse config
  │   ├── permissions
  │   │   ├── list  <id>
  │   │   └── set   <id>  --role <role>  --user <email>|--group <id>
  │   └── monitor
  │       ├── list    <dataset-id>
  │       ├── add     <dataset-id>        # column metric or custom SQL wizard
  │       ├── update  <dataset-id> <monitor-id>
  │       └── delete  <dataset-id> <monitor-id>
  │
  ├── incident
  │   ├── list    [--status open|closed|all] [--dataset <id>]
  │   ├── get     <id>
  │   └── update  <id>  [--status] [--note <text>]
  │
  ├── notification
  │   ├── list    [--channel <id>] [--dataset <id>]
  │   ├── add                             # wizard: channel → triggers → scope
  │   ├── update  <id>
  │   ├── delete  <id>
  │   └── channel
  │       ├── list
  │       ├── add   slack|teams|webhook
  │       ├── delete <id>
  │       └── test   <id>
  │
  ├── role
  │   ├── list    [--scope global|dataset]
  │   ├── create  --name <n>  --scope global|dataset
  │   ├── delete  <id>
  │   ├── show    <id>                    # permissions in this role
  │   ├── assign  <id>  --user <email>|--group <group-id>
  │   └── revoke  <id>  --user <email>|--group <group-id>
  │
  ├── users
  │   ├── list
  │   └── group
  │       ├── list
  │       ├── create  --name <n>  --members <emails...>
  │       ├── update  <id>
  │       └── delete  <id>
  │
  └── completion  bash|zsh|fish


UX Ideas

  1. No flag soup — smart defaults

  Current (soda-core):
  soda contract verify -ds ./configs/postgres.yml -c ./contracts/orders.yml -sc ./soda-cloud.yml --publish

  Proposed (with soda.yml project config):
  soda contract verify orders.yml --publish

  2. soda run — pipeline command

  A power command that orchestrates verify → publish → wait for scan completion → report:

  soda run orders.yml                   # verify + publish, stream results
  soda run orders.yml --agent           # via Soda Agent
  soda run ./contracts/ --parallel      # all contracts in dir
  soda run orders.yml --output json     # for CI/CD pipelines

  Exit codes: 0 = all checks pass, 1 = checks failed, 2 = error, 3 = auth error

  The mental model split:
  soda run → smart, project-aware, CI/CD and agents, accepts files or directories
  soda contract verify → explicit, all flags, no project context required (power users)

  3. soda status — instant dashboard

  $ soda status

    Organization   acme-corp
    Profile        production

    Datasets       142 total    3 failing
    Checks          87 passing   12 failing
    Incidents        2 open

    Last scan      orders_daily   2m ago   ✓ passed
                   users_weekly   1h ago   ✗ 3 failures → incident #47

  4. Consistent output flags (all commands)

  --output table     # default when TTY (human-friendly)
  --output json      # machine-readable (default when piped)
  --output csv       # for spreadsheet export
  --quiet            # suppress all output except errors
  --verbose          # debug-level detail
  --no-color         # for logs/CI
  --no-interactive   # fail instead of prompting (agent mode)

  5. First-class shell completion

  soda completion bash   >> ~/.bashrc
  soda completion zsh    >> ~/.zshrc
  soda completion fish   >> ~/.config/fish/completions/soda.fish

  Tab-completes dataset names, contract files, profile names by calling the API.


Auth & Config Architecture

  ~/.soda/
    credentials          # API keys per profile (never committed)
    config.yml           # Defaults, cloud host, profile selection

  ./soda.yml             # Project-level config (committed)
    └── datasources, contracts dir, default profile override